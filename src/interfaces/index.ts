// 示例（可删除，按自己具体项目需求更改）
export interface loginData {
  phone: string
  password: string
}

export interface userLoginReturn {
  avatar_url: string
  create_time: string
  phoneNumber: string
  roles: string[]
  sex: string
  token: string
  update_time: string
  username: string
}
